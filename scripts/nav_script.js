const navToggle=document.getElementById("nav_stack"),navMenu=document.getElementById("nav_box__menu"),navConfig=document.getElementById("nav_box__config"),originalNavMenuItems=Array.from(navMenu.children);navToggle.addEventListener("click",()=>{if(navMenu.classList.toggle("show"),navMenu.classList.contains("show")){let e=navConfig.cloneNode(!0).children;navMenu.append(...e)}else{let n=Array.from(navMenu.children),t=n.filter(e=>!originalNavMenuItems.includes(e));t.forEach(e=>e.remove())}});const sections=document.querySelectorAll(".bloque"),observer=new IntersectionObserver(function(e){e.forEach(e=>{if(e.isIntersecting){let n=e.target.id;updateActiveLink(n)}})},{rootMargin:"-20% 0px -80% 0px"});function updateActiveLink(e){let n=document.querySelectorAll("#nav_box__menu a");n.forEach(e=>{e.parentElement.classList.remove("active")}),n.forEach(n=>{let t=n.getAttribute("href");t.includes(`#${e}`)&&n.parentElement.classList.add("active")})}sections.forEach(e=>{observer.observe(e)});